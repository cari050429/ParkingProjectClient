@model ParkingProjectClient.Models.ParkingPermitDetails

@{
    ViewData["Title"] = "Parking Permit Details";
}

<div class="text-center">
    <h1 class="display-4">Parking Permit Details</h1>
</div>

@if (Model != null)
{
    <table class="table">
        <thead>
            <tr>
                <th>Effective Date</th>
                <th>Expiration Date</th>
                <th>Longitude</th>
                <th>Latitude</th>
                <th>Date Created</th>
                <th>Parking Area Type Description</th>
                <th>License Plate</th>
                <th>Parking Area Name</th>
                <th>Inactive</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>@Model.EffectiveDate.ToShortDateString()</td>
                <td>@Model.ExpirationDate.ToShortDateString()</td>
                <td>@Model.Longitude</td>
                <td>@Model.Latitude</td>
                <td>@Model.DateCreated.ToShortDateString()</td>
                <td>@Model.ParkingAreaTypeDescription</td>
                <td>@Model.LicensePlate</td>
                <td>@Model.ParkingAreaName</td>
                <td>@Model.Inactive</td>
            </tr>
            <tr>
                <td colspan="9" class="text-center">
                    <a href="@Url.Action("UpdateParkingPermit", "Home", new { id = Model.Id })" class="btn btn-warning">Update</a>
                    <a href="@Url.Action("DeleteParkingPermit", "Home", new { id = Model.Id })" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this parking permit?');">Delete</a>
                </td>
            </tr>
        </tbody>
    </table>
}
else
{
    <p>No parking permit details available.</p>
}
